import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "HelpGlobe - 3D Global Crisis Visualization",
  description: "Interactive 3D globe showing real-time global incidents including conflicts, natural disasters, and humanitarian crises with ways to help.",
  keywords: "3D globe, crisis map, humanitarian aid, disasters, earthquakes, global incidents, help organizations, real-time visualization",
  authors: [{ name: "HelpGlobe Team" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
